import React from "react";
import Cart from "../Cart/Cart";

const CartWidget = () => {
    return (
        <div className="mx-5">
            <Cart />
        </div>
    );
};

export default CartWidget;
