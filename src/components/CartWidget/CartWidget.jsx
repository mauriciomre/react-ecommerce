import React from "react";
import Cart from "../icons/Cart";

const CartWidget = () => {
    return (
        <div>
            <span>2</span>
            <Cart />
        </div>
    );
};

export default CartWidget;
